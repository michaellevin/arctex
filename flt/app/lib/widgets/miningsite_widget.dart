import 'package:flutter/material.dart';

class MiningSiteWidget extends StatelessWidget {
  const MiningSiteWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("MiningSiteWidget");
  }
}