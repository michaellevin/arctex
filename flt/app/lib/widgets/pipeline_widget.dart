import 'package:flutter/material.dart';

class PipelineWidget extends StatelessWidget {
  const PipelineWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("PipelineWidget");
  }
}