import 'package:flutter/material.dart';

class PipesectionWidget extends StatelessWidget {
  const PipesectionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("PipesectionWidget");
  }
}